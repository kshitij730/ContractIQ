{
    "name": "contract-iq",
    "version": "1.0.0",
    "private": true,
    "dependencies": {
        "next": "15.1.0"
    },
    "scripts": {
        "build": "cd frontend && npm install && next build"
    }
}